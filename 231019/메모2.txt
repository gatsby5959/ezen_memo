primary key(bno));

------------------------------------------------
VO => DB의 단일 테이블의 객체
DTO => 여러테이블을 묶어서 사용
화면 <=> ctrler  <=> service <=> dao <=> DB 저장

------------------------------------------------
handler => DB처리 필요 없이 화면과 컨트롤러의 처리가 별도의 처리가 필요할 때(페이징이나 파일처럼)
DB와 상관 없음.